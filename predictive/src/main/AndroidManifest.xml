<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.predictivemaintenence">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PredictiveMaintenence"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".activity.EditEquipmentActivity"
            android:exported="false" />
        <activity
            android:name=".activity.CreateEquipmentActivity"
            android:exported="false" />
        <activity
            android:name=".activity.SearchItemActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
    </application>

</manifest>